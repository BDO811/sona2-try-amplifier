name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it mid-publish.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npx tsc -p tsconfig.app.json --noEmit

      - run: npx vitest run

      # Serve from the Pages project subpath until try.amplifierhealth.com DNS
      # exists. Set this to "/" (and re-add the custom domain) once it does.
      - run: npm run build
        env:
          VITE_BASE_PATH: /sona2-try-amplifier/

      # The app uses BrowserRouter, so /detailed-analysis and friends are
      # client-side routes with no file behind them. GitHub Pages serves
      # 404.html for unknown paths; making it a copy of index.html lets the
      # router handle those URLs instead of showing Pages' own 404.
      - run: cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
